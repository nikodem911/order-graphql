type Order {
    id: ID!
    items: [OrderItem!]!
}

type OrderItem {
    id: ID!
    product: Product!
}

type Product {
    id: ID!
    name: String!
}

type Query {
    orders: [Order!]!
    products: [Product!]!
    items: [OrderItem!]!
}